# O build lê o Contentful e gera HTML. Estas duas variáveis só existem no build —
# a página no ar não tem runtime, então não tem segredo.
CONTENTFUL_SPACE_ID=
CONTENTFUL_DELIVERY_TOKEN=

# Só para o seed e as migrations (contentful/), nunca para o site.
CONTENTFUL_MANAGEMENT_TOKEN=
